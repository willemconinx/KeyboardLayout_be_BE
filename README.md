# KeyboardLayout_be_BE
Arduino KeyboardLayout_be_BE

add files to:   Arduino/libraries/Keyboard/src
add Keyboard.h AND  KeyboardLayout.h in code
And in script just enter  Keyboard.begin(KeyboardLayout_be_BE);
